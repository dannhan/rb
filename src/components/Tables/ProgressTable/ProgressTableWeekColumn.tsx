import { Input } from "@/components/ui/input";

type Props = { id: string };
const ProgressTableWeekColum: React.FC<Props> = () => {
  return <Input type="number" />;
};

export default ProgressTableWeekColum;
